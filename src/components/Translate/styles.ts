import { createStyles, makeStyles } from "@mui/styles";

const useStyles = makeStyles(() =>
  createStyles({
    container: {
      marginTop: "2em",
      textAlign: "center",
    },
  })
);

export default useStyles;
